import styled from "styled-components";

export const SourceWikiWrapper = styled.div`
  display: flex;
  align-items: center;
`;

export const SourceWikiTitle = styled.span`
  font-weight: 300;
  font-size: 1.2rem;
  color: var(--color-light-grey);
  margin-right: 0.4rem;
`;

export const SourceWikiAncor = styled.a`
  color: var(--color-light-grey);
  display: flex;
  align-items: center;
  font-size: 1.4rem;
  font-weight: 700;
`;
